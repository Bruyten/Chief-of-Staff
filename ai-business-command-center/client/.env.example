# =========================================================================
# Chief of Staff — Frontend env template
# Copy this to .env (client/.env) for local dev. NEVER commit a real .env.
# =========================================================================

# The base URL of your backend API.
# - Local dev:  http://localhost:4000
# - Production: https://chief-of-staff-api.onrender.com (or your custom domain)
#
# IMPORTANT: anything starting with VITE_ is COMPILED INTO THE BROWSER BUNDLE.
# Never put a secret here. Never. Not even "just for testing".
VITE_API_URL="http://localhost:4000"
